# File: backend/Dockerfile

# 1. Chọn hệ điều hành có sẵn Node.js
FROM node:18-alpine

# 2. Tạo thư mục làm việc bên trong container
WORKDIR /app

# 3. Copy file package.json vào trước (để cài thư viện)
COPY package*.json ./

# 4. Cài đặt thư viện
RUN npm install

# 5. Copy toàn bộ code còn lại vào
COPY . .

# 6. Mở cổng 3001 (Cổng của Backend)
EXPOSE 3001

# 7. Lệnh chạy server khi container khởi động
CMD ["node", "server.js"]